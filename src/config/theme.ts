import { createTheme } from "@mui/material";

export const appTheme = createTheme({
    palette: {
        mode: "dark",
        primary: { main: "#A5ABAF" },
        secondary: {  main: "#EBECCC" },
        text: { 
            primary: "#A5ABAF",
            secondary: "#EBECCC",
        }
    },
})
